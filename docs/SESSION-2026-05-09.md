# Session handoff — 2026-05-09

> Snapshot of where the v3.0 → v3.0.2 work landed and what's natural to
> pick up next session.

## Today's deliverables

**3 releases shipped** in one day:

| Tag | Theme | Commits | Critique items closed |
|---|---|---:|---:|
| `v3.0` | UI redesign + Swagger + Cite modal + i18n + Ephrem corpus | (existing) | (existing) |
| `v3.0.1` | Compliance & honesty hotfix | 4 | 17 |
| `v3.0.2` | Transparency, SEO, validation foundations, cross-script API fix | 7 | 7 |

Plus a follow-on commit (`05c0512`) adding a condensed Limitations &
Caveats section to `/about` in 4 languages.

**Aggregate: 24 of 47 critique items closed since the May 2026 review.**

## Production state

Everything is live at https://aramaic-root-atlas.onrender.com.
Local main, origin/main, and Render deploy all in sync at `05c0512`.

| Layer | Status |
|---|---|
| `/privacy` page (4 langs) + cookie banner + GA opt-in only | ✅ live |
| `LICENSE-DATA.md` per-corpus disclosure | ✅ live |
| OG/Twitter cards, JSON-LD, sitemap, robots, og-image.png | ✅ live |
| `docs/VALIDATION.md`, `docs/SEARCH-ALGORITHMS.md`, `CHANGELOG.md` | ✅ in repo |
| Diachronic-page caveat banner (4 langs) | ✅ live |
| `/about` Limitations & Caveats section (4 langs) | ✅ live |
| Cite modal: `@software` BibTeX, canonical URL, all 5 formats fixed | ✅ live |
| Cross-script `/api/roots` (Latin/Syriac/Hebrew/Arabic) | ✅ live (was broken) |
| Test suite: 15 → 150 tests passing | ✅ in repo |
| Watch Video disabled with re-record notice | ✅ live |
| Confidence scoring honest framing on /about | ✅ live |
| Aramaic stems vs Hebrew binyanim disambiguation | ✅ live |
| TipTopJar demoted to footer | ✅ live |

## Items still open from the critique (23 of 47)

### Decisions waiting for you

1. **Atlas-name framing (C2.4 / Roadmap 1.4)** — Keep "Atlas" branding and add
   real geographic data (manuscript provenance maps, dialect distribution),
   or rebrand to "Concordance + Visualizer"?
2. **Institutional partner (Roadmap 3.1)** — Which organization to approach
   first? Candidates: VU Amsterdam (ETCBC), Hebrew Union College (CAL),
   Beth Mardutho, Notre Dame, Leiden, Tübingen. Have an outreach email
   draft ready?
3. **Hebrew/Arabic UI native review (Roadmap 3.4)** — Do you have candidate
   reviewers, or want me to draft an outreach pitch?
4. **JOSS submission target (Roadmap 3.5)** — End-of-Phase-2 (after gold
   standard + P/R) or wait for named partner?
5. **Long-term hosting (Roadmap 4.5)** — Stay solo on Render Pro, or seek
   an institutional anchor (university lab page) once Phase 3.1 lands?
6. **Mandaic in v4 (Roadmap 6.8)** — Include in next major (significant
   multi-script engineering lift), or keep as stretch goal?

### Items that need your domain expertise (autonomous work blocked)

| ID | Item | Effort | Why blocked |
|---|---|---|---|
| **2.0** | Non-triliteral root pattern classes (geminate, hollow, weak, quadriliteral) | L | Invasive; changes extraction logic on every existing analysis. Needs expert review of behaviour change. |
| **2.1** | Gold-standard test set (300 hand-annotated verses, 60 per corpus) | L | Requires Aramaic-specialist annotation; can't do without you. |
| **2.2** | Precision/recall study | M | Depends on 2.1. |
| **2.4** | Cognate audit against HALOT, BDB, Sokoloff, Brockelmann, Lane, Wehr | XL | Months of lexicographer work; ~1,127 entries × 6 lexicons. |
| **2.5** | Confidence calibration | M | Depends on 2.1. |

### Items I can do autonomously next session

| Phase | Items | Estimated session time |
|---|---|---|
| **Phase 4 — Infrastructure** | API versioning (`/api/v1/...`), rate limiting (Flask-Limiter), accessibility audit (axe-core), succession plan doc, real persistence migration plan for annotations | 1-2 sessions |
| **Phase 2 partial** | Genre-control UI on diachronic (already disclosed; UI control still pending — item 2.8). Editorial chronology dropdown (item 2.9, partially). | half session |
| **Phase 5 — Refactoring debt** | Extract `t()` helper to replace inline 4-language conditionals, split `app.py` into per-feature blueprints, cite-modal SBL polish | 1 session |

## Natural next action

The single highest-leverage next move is **Phase 4.4 — accessibility audit**.
Run `axe-core` against every page; fix WCAG AA violations (color contrast,
ARIA labels, keyboard nav, screen reader semantics for the visualizer's
SVG). This is purely engineering work, doesn't need expert input, and
removes a major institutional-adoption blocker (a11y compliance is often
gating for university IT review).

Alternative starter for next session: **Phase 4.6 — succession plan doc**
(`docs/SUCCESSION.md`). Smallest possible item, half hour, but eliminates
the "single point of failure" critique (C2.25) for free.

## Open follow-ups noticed but not actioned

- **`/api/stats` reports `root_count: 5249`** but the README says **5,039**.
  One of these is stale. Worth a quick reconciliation pass — probably the
  README number is from an earlier release.
- **The `og:title` falls back to the page `<title>`** (e.g. "Trace Root —
  Aramaic Root Atlas"). For social shares of the homepage specifically,
  a more compelling og:title like "Aramaic Root Atlas — Cross-corpus
  triliteral root explorer" would convert better.
- **The 16 stale screenshots backlog item** is still in TODO — the README
  now has only the homepage hero + a redirect to live app + tour. Could
  be reasonable to leave as is; or recapture 4 hero screenshots
  (visualizer, reader, concordance, diachronic) when the validation work
  lands.

## How to resume tomorrow

```bash
cd "/Users/jfresco16/Google Drive/Claude/aramaic-root-atlas"
git pull origin main          # should be a no-op; just confirm sync
PORT=5002 python3 app.py &    # start dev server
python3 -m pytest tests/      # verify 150 tests still green
```

Then open `docs/ROADMAP-v3.1.md` for the full plan, or this file for
the day-N+1 view.

---

*Generated 2026-05-09 end-of-day. Update or delete on next session.*
